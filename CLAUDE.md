@AGENTS.md

# hotel-bargain — 開発ガイド

**重要: このディレクトリのコード(Next.js)は現在使われていない。**
実体は `/Users/kitaryo/Projects/hotel-bargain-php`（PHP、ConoHa WING）に完全移行済み。
本番は `https://bargain.hotelx.tech`。このディレクトリはVercel版として最初に作ったが、
「もともとこのアプリで実現したかったこと」をConoHa WING上のPHPで作り直すことになり、
役目を終えた。新しいセッションがこのディレクトリを開いた場合、まず
`/Users/kitaryo/Projects/hotel-bargain-php` の状態を確認すること。

**hotel-bargain-php は GitHub の [ryokita001-cpu/hotel-bargain-php](https://github.com/ryokita001-cpu/hotel-bargain-php)（Private）でGit管理されている**（2026-08-29にgit init & 初回push済み）。
`main`が本番相当。変更はfeatureブランチを切ってPR経由で`main`にマージする運用にした
（詳細は6章）。

---

## 1. プロジェクトの目的・全体概要

hotel-bargain.com は、楽天トラベルの空室検索APIをもとに「過去の相場と比べて
いま価格が下がっているホテル」を検出し、エリア・日付・下落率で探せるポータル。
収益源は楽天トラベルへのアフィリエイト。安さの推定理由は曜日・宿泊日までの日数・
季節性・下落率からルールベースのテンプレートで1〜2文生成する(2026-08-29に
Gemini API生成から移行、3章参照)。

**現在のサイトコンセプトは「ふらっと東京ひとり旅」**(2026-08-29に決定、変えては
いけない設計)。対象エリアは東京23区内9エリアのみ、期間は直近1週間、表示価格は
大人1名・1部屋あたり。全国展開・複数人旅行ではない(3章参照)。

## 2. これまでに完了したこと

- Next.js/Vercel/Supabase/GitHub Actions構成で一度MVPを構築し、実際に本番稼働まで
  持っていった（Vercel Hobbyのcron頻度制限・関数実行時間制限に何度もぶつかった）
- その後、常時稼働サーバーでcrontabを自由に使える方が本質的に楽という判断で、
  ConoHa WING上のPHP + MySQLへ完全移行（`hotel-bargain-php`）
- 楽天Web ServiceのAPI仕様をドキュメントに頼らず実機検証し、`hotel-bargain-php/lib/RakutenClient.php`
  に反映（詳細は3章）
- レビュー数によるホテルの事前スクリーニング(`cron/discover_hotels.php`)を実装。
  `hotels.last_selected_at`で選外ホテルを絞り込む設計にした(3章参照)
- SEO対応(robots.txt, 動的sitemap.xml, canonical, 薄いページのnoindex, JSON-LD)
- GA4(測定ID `G-3MTPE9C50K`)のトラッキングタグを`index.php`/`deals.php`に設置し、
  カスタムイベントを発火する仕組みを追加、本番デプロイ済み（イベント名は
  2026-08-30に用途別へ作り直した。受信確認は未実施、3章・4章参照）
- hotel-bargain-phpをGitHub(Private)でGit管理下に置き、feature branch→PR→main
  マージの運用を開始した(3章参照)
- 2026-08-29、3日間ブロックされていた楽天app1を復旧(原因はApplication URLの
  不一致。詳細は3章「楽天APIのレート制限とマルチアプリ運用」)
- **2026-08-29、マーケ判断によりサイトコンセプトを全面的に転換した**（詳細は3章の
  各セクション参照）:
  - 対象を全国9エリアから東京23区内の全9エリアに集中させ、地理的網羅率・検出精度を
    優先する構成にした。他7エリアはコードを一時停止(データは保持)
  - 楽天APIアプリを2本から5本に拡張
  - 期間を「直近1〜2週間」から「直近1週間」に短縮し、浮いた分の網羅率(ホテル数)を
    12件/エリア→30件/エリアに引き上げた
  - 月表示カレンダー機能(`calendar.php`)を廃止し、直近7日の日付タブに統合
  - エリア選択UIを、実際の東京23区の形をしたSVG地図に刷新(3章参照)。エリアごとの
    Deal件数を地図上に出し、日付タブと連動させた
  - 割引率フィルタをプルダウンからスライダーに変更
  - 安さの理由文生成をGemini APIからルールベースのテンプレート選択に変更
    (Gemini無料枠のレート制限に頻繁に引っかかるようになったため)
  - 表示価格が「大人1名・1部屋あたり」であることを明記し、サイト文言を
    「ふらっと東京ひとり旅」の訴求に統一
  - `fetch_prices.php`・`detect_deals.php`が本日チェックインの宿泊を一切検出
    できていなかった不具合を発見・修正(Deal件数が4件→39件に増加して確認)
  - `deploy/conoha-sync.sh`に`rsync --delete`を追加し、ローカルで削除した
    ファイルが本番に残り続ける問題を解消
  - 「直近相場」が実売価格ではなく自社推定値であることを明記する注記を追加
    (景品表示法対応)
- **2026-08-29、SEOを目的にサイト構成を作り直した**(詳細は3章):
  - エリア別の正規ページ`/area/{slug}/`を9本新設。エリア解説・主な駅・向いている
    滞在に加え、DBから算出した「このエリアの価格の目安」を載せ、1ページ1,600字程度
    の独自コンテンツにした
  - `/about/`(相場の算出方法とFAQ)・`/404.php`を追加
  - head/ヘッダー/フッター/パンくずを`public/partials/`に共通化。全ページから
    9エリアに辿れる内部リンクを作った
  - 構造化データを WebSite / BreadcrumbList / FAQPage / ItemList に拡充
  - `.htaccess`でURLを正規化(`/index.php`→`/`、`/area/xxx`→`/area/xxx/`)
  - Tailwind CDNをやめてビルド済みCSSに移行(3章「CSSはビルドが必要」参照)
  - ホテル画像を楽天CDNの200px版に差し替え、1枚420KB→38KBに削減
  - ファビコン(`public/favicon.svg`がマスター)とOGP画像を追加
- 2026-08-29、アフィリエイトリンクをホテル単位からプラン単位に変更し、
  カードに朝食の有無を表示するようにした(3章参照)
- 2026-08-29、宿一覧に並び替え(相場との差が大きい順/料金が安い順)を追加
- 2026-08-29、日付タブに本日を含めて8日分にした(それまでdealの大半を占める
  本日分にタブから辿り着けなかった。3章参照)
- 2026-08-29、Google Search Consoleへの登録とサイトマップ送信が完了した
  (DNSのTXTレコードで所有権確認。`public/partials/head.php`のmetaタグは
  URLプレフィックス型でも確認できるようにした保険で、不要なら消してよい)
- **2026-08-30、全体レビューで見つかった不具合を修正した**(詳細は3章):
  - 相場と現在価格の比較が「全プランの中央値 vs 特定1プラン」という
    噛み合わない組み合わせだったのを、両方「その日の最安値」に揃えた
  - 相場を曜日別(7分類)から平日/休前日(2分類)に変更。判定できる割合が
    19.4%→35.1%、東京都のdeal件数が11件→145件になった
  - 乖離率に上限(60%)を設けた。運用初期のbaselineの推定ぶれ対策
  - 0:00〜5:30(cron実行前)に前日のdealが表示されていたのを、表示側でも
    `checkin_date >= CURDATE()`で足切りするようにした
  - GA4のイベントを用途別に分け、成果地点である楽天への遷移
    (`select_hotel`)を計測できるようにした
  - カード全体をクリックできるようにした(写真とホテル名が無反応だった)
  - トップとエリア一覧で同じホテルの別日が並ばないようにした
- **2026-09-01、楽天アフィリエイトの収益導線を実機検証した**(詳細は3章
  「アフィリエイトURLは『その価格のプラン』に、iPhoneアプリでも日付付きで
  飛ばす」参照)。ユーザーが楽天アフィリエイト管理画面でクリックの発生
  (8/26以降、直近ピークで32件/日)を
  確認したのがきっかけ。curlでリダイレクトを追跡し、`hb.afl.rakuten.co.jp`→
  `pt.afl.rakuten.co.jp/c/{affiliateId}/?url=...`(楽天のクリック計測)→
  プランページ、という正しいアフィリエイト導線であることを確認した。
  その過程で、2026-08-29にmainへマージ済みだった`booking_url`の
  `planListUrl`優先化(コミット`1880790`)が**本番に未デプロイのまま**
  だったことが発覚し、`deploy/conoha-sync.sh`で本番へデプロイして解消した
  (4章参照)
- **2026-08-31〜09-04、iPhoneで楽天トラベルアプリが起動したときにリンクが正しく
  機能しない問題を、4回作り直してようやく解決した**(詳細は3章「アフィリエイトURLは
  『その価格のプラン』に、iPhoneアプリでも日付付きで飛ばす」参照)。
  `m`パラメータの反映漏れ(#33) → JSでのアプリ起動回避(効かず、#34/#35) →
  `booking_url`をAPIのURLフィールドではなくホテル番号から自前で組み立てる方式へ
  変更してホテル特定を修正(#36〜#38) → 着地先を施設ページからプランページに変えて
  日付を反映(#41/#42)、という順。**アプリ起動を止めるのではなく、アプリが解釈できる
  URL形式に合わせるのが正解だった**

## 3. 技術構成・設計方針・重要なルール

### 実体はhotel-bargain-php

- 本番: `https://bargain.hotelx.tech`（ConoHa WINGアカウント `c2870339`）
- DB: MySQL (`mysql68.conoha.ne.jp` / `c9qmf_bargain`)
- ローカル開発: `config.php`（gitignore済み）にDB/API資格情報を書く。
  本番用は `deploy/config.production.php`（同じくgitignore済み）に書き、
  `bash deploy/conoha-sync.sh` でrsyncデプロイする

### ローカルDBと本番DBは別物(重要な罠、2026-08-29に実際に混乱した)

ローカルの`config.php`は`DB_HOST=localhost`の別DB(`hotel_bargain`)に接続しており、
本番DB(`mysql68.conoha.ne.jp`/`c9qmf_bargain`)とは完全に別データ。**ローカルから
`discover_hotels.php`等を実行しても本番には一切影響しない。** 本番の状態を確認・
変更するには、SSH(`ssh -i ~/.ssh/conoha-borutomo.pem -p 8022 c2870339@www323.conoha.ne.jp`)
で直接ログインしてログ(`~/hotel-bargain/logs/*.log`)やDBを見る必要がある。
2026-08-29に「price_logsが3日前で止まっている→本番cronが止まっている」と誤診断
しかけたが、実際はローカルDBの古いデータを見ていただけで、本番は正常に稼働していた。
**「ローカルで確認できた」は「本番で確認できた」を意味しない。** 本番の動作確認は
必ずSSH経由で行うこと。

### Git運用

- リポジトリ: [ryokita001-cpu/hotel-bargain-php](https://github.com/ryokita001-cpu/hotel-bargain-php)(Private)
- `config.php` / `deploy/config.production.php`は`.gitignore`済み(資格情報を含むため)
- 変更は`main`に直接コミットせず、featureブランチ→PR→マージの流れで行う
  (2026-08-29にGA4タグ導入をこの流れで実施)
- git init時点のコード(GA4タグ導入前)がベースラインとして`main`の初回コミットに
  なっている

### 東京集中方針(2026-08-29、マーケ判断・変えてはいけない設計)

全国9エリアから、東京23区内の全9エリア(`smallClassCode: tokyo`の
`detailClassCode A〜I`、GetAreaClassで実機確認済み)に対象を絞った。全国展開より
東京での地理的網羅率・検出精度を優先する判断。

- `lib/Areas.php`の`seed_areas()`で東京9エリアのみ返すようにし、他7エリア
  (神奈川・愛知・京都・大阪・福岡・北海道・沖縄)はコメントアウトして一時停止
  (削除ではない。hotels/price_logs等のデータも保持したまま。再開時はコメントを
  外すだけでよい)
- フロント表示側(index.php/deals.php/sitemap.php/`lib/Deals.php`)は同ファイルの
  `DISPLAY_PREFECTURE`定数(現在'東京都')でSQLを絞り込んでいる。**`seed_areas()`
  だけ戻してもフロントには出ない。エリアを増減する際は`seed_areas()`・
  `DISPLAY_PREFECTURE`・地図の形状データ(`lib/TokyoAreaMap.php`)の3つを揃えて
  変更すること**(形状データが無いエリアは地図に描かれず、下のプルダウンから
  しか選べない状態になる)
- エリア選択UI(`public/partials/area_map.php`)は、DBの`area_id`が環境
  (ローカル/本番)で採番が変わりうるため、`seed_areas()`で定義した固定の
  エリア名をキーに地図の形状を紐付けている。**idではなく名前でマッチさせる
  設計を崩さないこと**（idでハードコードすると本番とローカルでズレて壊れる）

**過渡期の注意**: 2026-08-29のデプロイ以前に走った通常cron(4:00)は旧設定
(全国9エリア)のままだったため、他7エリアの`hotels.last_selected_at`がまだ新しい
(`SELECTION_FRESHNESS_DAYS=2日`以内)。2026-08-30頃まではfetch_prices.php/
detect_deals.phpが他エリアも引き続き処理し、is_active=1のdealsも作られる
(フロントは`DISPLAY_PREFECTURE`絞り込みで表示されないため実害はない)。
2026-08-31以降は他エリアのhotelsが完全にfreshでなくなり自然に対象外になって
いるはずなので、**次回セッションで`SELECT prefecture, COUNT(*) FROM hotels h
JOIN areas a ON a.id=h.area_id WHERE last_selected_at >= NOW() - INTERVAL 2 DAY
GROUP BY prefecture`等で東京都以外が0件になっているか確認すること**（本番DBに
対して。上記「ローカルDBと本番DBは別物」参照）。

### サイトコンセプトは「ふらっと東京ひとり旅」(2026-08-29、変えてはいけない設計)

表示価格は`RakutenClient.php`の`adultNum`/`roomNum`のデフォルト値(共に1)による
「大人1名・1部屋あたり」の料金。これに合わせてサイト全体を一人旅・出張向けの
訴求に統一した(index.php/deals.phpのtitle・見出し・説明文、`hotel_card.php`に
「大人1名・1部屋あたりの料金」の注記)。「お得に二人旅」の方向に転換する場合は
`adultNum=2`への変更とAPI呼び出し・文言の再調整が必要になる(未実装)。

### エリア選択マップの設計(2026-08-29、変えてはいけない設計)

エリア選択は、実際の東京23区の形をしたインラインSVGの地図
(`public/partials/area_map.php`、形状は`lib/TokyoAreaMap.php`)。「泊まりたい場所」を
地理的な位置関係から選べるようにするのが目的で、位置関係を模式的に示すだけだった
3x3グリッド+絵文字アイコンを置き換えたもの。

- 区割りは楽天トラベルの東京23区マップ
  (`https://travel.rakuten.co.jp/yado/tokyo/map_s.html`)のimage mapのポリゴン座標を
  簡略化して起こした(その9区画が楽天APIの`detailClassCode` A〜I＝`seed_areas()`の
  9エリアと完全に一致することを実機で確認済み)。座標系は元の地図と同じ600x600
- **隣り合うエリアは境界の頂点列を共有している。**頂点を編集するときは隣接エリア側の
  同じ頂点も必ず同時に直すこと(片方だけ動かすとエリア間に隙間や重なりが出る)
- 各エリアは`deals.php?area=N`への`<a>`。旧UIのようにJSでform submitしていないので
  JS無効環境でも動き、内部リンクとしてクロールもされる。日付タブと同じ
  `select_area`(GA4)を発火する
- 地図には**エリアごとのDeal件数**を出し、宿があるエリアは白、0件は薄いグレーで
  塗り分けている(日付タブで日を選ぶと件数・色も連動する)。**cronが9エリア分
  正常に回ったかは、この地図の色を見れば一目で分かる**(白＝取得できている、
  グレーのまま＝そのエリアが取れていない可能性)

### SEOのページ設計(2026-08-29、変えてはいけない設計)

検索の受け皿は**エリア別ページ`/area/{slug}/`**(`public/area.php`)に寄せている。
一覧だけのページでは「新宿 ホテル 安い」のような検索で戦えないため、
`lib/AreaContent.php`のエリア解説・主な駅・向いている滞在と、DBから算出した
「このエリアの価格の目安」(直近1週間の最安・中央値・追跡中の宿数)を組み合わせ、
Dealが0件の日でもページに中身が残るようにしてある。

- `lib/AreaContent.php`のキーはDBの`area_id`ではなく`seed_areas()`のエリア名
  (`lib/TokyoAreaMap.php`と同じ理由)。**エリアを増減する際は`seed_areas()`・
  `DISPLAY_PREFECTURE`・`TokyoAreaMap.php`・`AreaContent.php`を揃えること**
- **`deals.php?area=N`はcanonicalをエリアページに向けて評価を集約している。
  ここにnoindexを併記してはいけない。**「このページを見るな」と「評価はあちらに
  寄せてくれ」が矛盾し、canonical先のエリアページまでインデックスされなくなる
  恐れがある(一度書いてしまい、気づいて外した)。日付・乖離率・並び順つきのURLは
  内容の薄い重複ページなので従来どおりnoindexでよい
- URLの正規化は`public/.htaccess`で行う(`/index.php`→`/`、`/area/xxx`→
  `/area/xxx/`をいずれも301)。**リライトはPHPビルトインサーバーでは効かないため、
  ローカルでは`area.php?slug=xxx`で確認し、リライト自体は本番で確認すること**
- sitemapはエリアページ中心(`public/sitemap.php`)。絞り込み後のURLは載せない

### 直近1週間・週間UIの設計(2026-08-29、変えてはいけない設計)

- `LOOKAHEAD_DAYS`(`cron/fetch_prices.php`・`cron/detect_deals.php`)は14→7日に
  短縮。浮いたAPI呼び出し予算を`TOP_N_PER_AREA`(`cron/discover_hotels.php`)
  12→30に振り向け、ホテル網羅率を優先している
- **本日から始めるループを`$day=1`/`$i=1`に戻さないこと。**2026-08-29に
  同じ種類の抜けを2回踏んでいる:
  - `fetch_prices.php`・`detect_deals.php`が`$day=1`からで、本日チェックインの
    宿泊が一切検出されなかった(本番の「今週のイチオシ宿」に本日チェックインの
    古いdeal＝reason_textが空の残骸が残っていたことから気づいた。
    修正後Deal件数が4件→39件に増加して確認)
  - `build_week_days()`(`lib/CalendarView.php`)が`$i=1`からで、**データとしては
    存在する本日分のdealに日付タブから辿り着けなかった**。発覚時点の本番は
    東京都のdeal 11件中10件が本日チェックインで、一覧の入り口から最も件数の
    多い日が丸ごと抜け落ちていた。修正して本日〜+7日の8日分にした
  - 前者を直したときに後者に気づけなかったのは、cron側とUI側で別々に
    日付範囲を組み立てているため。**片方を直すときは必ずもう片方も確認すること**
- 月表示カレンダー機能(`calendar.php`)は廃止・削除済み。直近7日の日付タブ
  (`public/partials/week_tabs.php`)に統合し、`index.php`/`deals.php`両方から使う
- 割引率フィルタはプルダウンではなくスライダー(25〜60%、5%刻み)。デフォルト値25
  の間は`minDiscount`パラメータをURLに含めないようJS側で制御している。これは
  `robots.txt`の`Disallow: /*?*minDiscount=`によるSEO除外設定を無意味に広げない
  ため。**スライダーの実装を変える際もこの「デフォルト値はURLに出さない」挙動を
  維持すること**

### 楽天Web Service APIの実機検証済み仕様(ドキュメントに無い部分)

- `applicationId`・`accessKey`はクエリパラメータとして両方必須
- **Refererヘッダーが、使用するアプリの登録済みApplication URLと完全一致しないと
  403(`REQUEST_CONTEXT_BODY_HTTP_REFERRER_MISSING`または`HTTP_REFERRER_NOT_ALLOWED`)
  になる。末尾スラッシュの有無も含めて完全一致が必要**(2026-08-29に実際に確認。
  詳細は次項参照)。現在5アプリとも`https://bargain.hotelx.tech/`で登録している
  (以前はapp1だけ実在しない`https://hotel-bargain.com`のままだった)
- レスポンス(`VacantHotelSearch`, `formatVersion=2`)の`hotels`は「1ホテル分のエントリ配列」が
  そのまま要素になった配列の配列。`{hotel: [...]}`という包み方ではない
- `roomInfo`配下は`{roomBasicInfo}`と`{dailyCharge}`が**別要素として交互に**並ぶ
  (1プランにroomBasicInfoが1つ、宿泊日数分のdailyChargeが続く)。1つのオブジェクトに
  両方入っているわけではない
- 緯度経度は標準の10進度ではなく、**度*3600の秒単位**で返る
  (`rakuten_coord_to_degrees()`で必ず/3600すること。しないとDBのDECIMAL(9,6)が桁あふれする)

- `roomBasicInfo`には`withBreakfastFlag`/`withDinnerFlag`(食事の有無)と
  `planId`(楽天側の`f_camp_id`)が含まれる。`price_logs`/`deals`に保存して
  カードの「朝食付き」バッジとプラン単位リンクに使っている
- **`adultNum`を変えると別リクエストになる。**レスポンスに入るのは指定人数に
  対応する単一の価格だけで、1名分から2名分を導くことはできない。したがって
  2名宿泊版を作るならAPI使用量は倍になる(2026-08-29に実機で確認)
- `dailyCharge.total`は**1室あたりの合計金額**(`roomNum`を2にしても値が変わらない
  ことで確認)。`rakutenCharge`が1人あたり。現在の`adultNum=1`表示は
  「大人1名・1部屋あたり」で正しい
- ホテル画像には縮小版がある: `/HIMG/{90|200}/{hotelNo}.jpg`。**90と200以外の
  サイズは404**。`hotelImageUrl`は最大2000px・1枚420KBのことがあり、一覧に並べると
  重すぎるため、エリアページでは200px版(約38KB)を使い、onerrorで元画像に
  フォールバックさせている(`rakuten_hotel_thumbnail_url()`)

### 楽天APIのレート制限とマルチアプリ運用(変えてはいけない設計)

- 秒間1リクエストは公式に明記されているが、**それとは別に、未審査アプリには
  非公開の1日あたり累積リクエスト上限がある**(2026-08-26に実際に確認)。超えると
  `HTTP_REFERRER_NOT_ALLOWED`という紛らわしいエラーを返す(Refererの設定ミスではなく、
  量的な一時ブロック)。継続すればアプリID自体が停止するリスクもあるとされる
- 対策として`RAKUTEN_APP1_*`〜`RAKUTEN_APP5_*`の5アプリ(2026-08-29に2→5本へ拡張)
  をconfig.phpに持ち、`rakuten_pick_credentials($index)`でバッチ・エリアごとに
  順に選択している(`lib/RakutenClient.php`の`rakuten_credential_pool()`)。
  Application URLは全アプリ`https://bargain.hotelx.tech/`、Affiliate IDも全アプリ
  共通(`547b5c8c.497b96a4.547b5c8d.1a3a86b8`)。**どれか1つが本番稼働中に
  ブロックされることは前提として起こりうる。** その場合は`deploy/config.production.php`
  の該当APP側を一時的に別の生きているアプリと同じ値に書き換えて凌ぎ、復旧を
  確認したら必ず元の値に戻すこと（2026-08-26に実際にこの手順で対応した。
  戻し忘れるとマルチアプリ運用の意味が無くなる）
- **2026-08-26〜29の3日間app1がブロックされ続けた真因は、上記の累積上限ではなく
  Application URLの不一致だった。** app1は登録Application URLが実在しない
  `https://hotel-bargain.com`(旧Next.js版、Vercelから既に外れている)のままで、
  Refererヘッダーと完全一致しない状態がずっと続いていた。楽天デベロッパーズ管理画面で
  `https://bargain.hotelx.tech/`(app2と同じ、末尾スラッシュも含めて完全一致)に
  変更し、`config.php`/`deploy/config.production.php`の`RAKUTEN_APP1_APPLICATION_URL`
  も合わせたところ即座に復旧した。**`HTTP_REFERRER_NOT_ALLOWED`が出たら、まず
  累積上限ではなくApplication URLの完全一致(末尾スラッシュ含む)を疑うこと。**
  なお累積上限による一時ブロックの可能性自体は否定されたわけではないので、
  今後また起きた場合はURL一致を確認した上で両方の可能性を検討する

### アフィリエイトURLは「その価格のプラン」に、iPhoneアプリでも日付付きで飛ばす(2026-09-04実機検証、変えてはいけない設計)

2026-08-31〜09-04にかけて、ここは4回作り直している(#33〜#42)。**同じ罠を踏み直さない
ために、なぜ今の形なのかを順に書く。**

- `hotels.booking_url`は「アフィリエイトIDを踏む中継URL」
  (`https://hb.afl.rakuten.co.jp/hgc/{アフィリエイトID}/?pc=<遷移先URL>&m=<遷移先URL>`)。
  `lib/RakutenClient.php`の`rakuten_wrap_affiliate_url()`で自前でラップしている
- **楽天APIが返すURLフィールド(`hotelInformationUrl`/`planListUrl`)は使わないこと。**
  ホテルによってはどちらも`img.travel.rakuten.co.jp/image/tr/api/re/{コード}/?f_no=...`
  という追跡用の中継URLになっており、楽天トラベルアプリのディープリンクがこれを
  解釈できず、iPhoneでアプリが起動すると該当ホテルが特定されない画面が開いた
  (2026-09-02に実機で確認)。ホテル番号から正規URLを自前で組み立てる
  (`rakuten_hotel_page_url()` / `rakuten_plan_page_url()`)
- **着地先はプランページ`hotel.travel.rakuten.co.jp/hotelinfo/plan/{ホテル番号}`。**
  ホテル番号はクエリ(`?f_no=`)ではなく**パス末尾**に置く(楽天トラベル自身がサイト内
  リンクで使っている形式)。ここに`f_nen1/f_tuki1/f_hi1`・`f_nen2/f_tuki2/f_hi2`・
  `f_otona_su`・`f_heya_su`・`f_camp_id`(プランID)・`f_flg=PLAN`を付ける
- **施設ページ(`travel.rakuten.co.jp/HOTEL/{no}/{no}.html`)に飛ばしてはいけない。**
  あちらは日付を**JavaScriptで反映する作り**で、サーバー応答には現れない。ブラウザ
  では日付が入るのに、**アプリはそのJSを通らないため日付がブランクになる**。
  プランページはサーバー側で`f_nen1`等を解釈するので、アプリでも日付が入る。
  2026-09-04にiPhone実機で同じ宿・同じ日付を踏み比べた結果:

  | 着地先 | アプリ起動 | 日付 |
  |---|---|---|
  | 施設ページ`/HOTEL/{no}/{no}.html` | する | 入らない |
  | プランページ`/hotelinfo/plan/{no}` | する | **入る** ★採用 |
  | プランページ + `l-id` | しない | 入る |
  | 施設ページ + `l-id` | する | 入らない |

- **`pc`(PC向け)と`m`(モバイル向け)の両方を必ず同じ内容にすること。** iPhoneのSafariは
  モバイルUAとして扱われるため、`m`を書き換え忘れるとスマートフォンだけ日付・プラン
  未指定のURLに飛ぶ(2026-08-31、#33)
- **`pc`/`m`の中身以外を書き換えるとアフィリエイトIDを踏まなくなり収益に影響する。**
  中身(遷移先URL)自体は任意の楽天トラベルURLに差し替えてよく、
  `hb.afl.rakuten.co.jp` → `pt.afl.rakuten.co.jp/c/{アフィリエイトID}/`(楽天のクリック
  計測ドメイン) → 着地ページ、と正しく遷移することを2026-09-04にcurlで確認済み
  (着地URLには楽天側が`scid=af_sp_etc&sc2id=af_101_0_0`を付ける)
- **universal link(アプリ起動)はJSでは回避できない。** タップをJSで一度受けてから
  遷移させる回避策(#34)は効かず、削除した。判定はページ遷移時にOS側で行われるため。
  URL側で外したい場合は`l-id`を付ける(アプリのAASA
  `https://travel.rakuten.co.jp/.well-known/apple-app-site-association`が
  `{"/": "/*", "?": {"l-id": "?*"}, "exclude": true}`をcomponentsの先頭に持ち、
  componentsは**先頭から最初にマッチした要素が勝つ**)。ただし施設ページでは`l-id`を
  付けてもアプリが開いたので、**アプリ抑止に頼る設計にはしないこと**。
  `RAKUTEN_FORCE_WEB_LANDING`(現在`false`)が、将来アプリ側の挙動が変わった場合の
  逃げ道として残してある
- **`roomBasicInfo.reserveUrl`は使わないこと。** これは予約フロー直行のURLで、
  踏むと楽天会員のログイン画面が出る。「詳細を見る」から突然ログインを求めるのは
  離脱を招くため採用しなかった(実機で確認済み)
- プランが売り切れていた場合もエラーにはならず、同じホテルのプラン一覧に
  「ご指定の条件での空室が見つかりません」と出るだけなので、日付を変えて探せる
- `deals.plan_id`は`current_price`を出したのと**同じ`price_logs`の行から取ること**。
  表示した価格とリンク先のプランを一致させるため(`cron/detect_deals.php`)
- 想定外の形式(`pc`が無い等)の場合は元のURLをそのまま返すフォールバックにしてある。
  旧形式(`?f_no=`付きの中継URL)の`booking_url`が残っていても、表示時にホテル番号を
  取り出して同じ正規URLに揃える(`rakuten_extract_hotel_no()`)
- **「mainにマージした」は「本番で動いている」を意味しない。** 2026-08-29に
  mainへマージした`booking_url`の`planListUrl`優先化が、2026-09-01時点で
  **本番に未デプロイのまま**だったことが実機検証で発覚した。
  `deploy/conoha-sync.sh`を明示的に実行するまで本番の挙動は変わらない。
  「直したはず」の変更ほど、SSH経由で本番の実ファイルをgrep等で見て反映を
  確認する習慣をつけること(3章「ローカルDBと本番DBは別物」と同種の罠)
- **iPhone実機でしか分からないことがある。** アプリがどのURL形式まで解釈するかは
  楽天の非公開実装で、curlやヘッドレスブラウザでは再現できない(施設ページは
  ヘッドレスブラウザだと日付が入って見える)。この種の切り分けは、候補URLを並べた
  使い捨てページを本番に置いてユーザーに踏んでもらうのが最短だった

### 「割引」「OFF」「セール」は使わない(景品表示法対応、変えてはいけない)

比較対象(`baseline_price`)はホテルの実売価格ではなく、自社算出の相場推定値
(過去60日・同一曜日の中央値)。これを「割引」と表示すると、消費者庁の二重価格表示
規制(有利誤認表示)に抵触するリスクがある。UI文言・理由文テンプレート
(`lib/DealReason.php`)とも「相場より○%安い」で統一する。加えて、宿一覧ページには
「直近相場」が実売価格ではなく自社推定値であることを明記する脚注を表示している
(deals.php/index.php)。

### 相場と現在価格は「その日の最安値」で揃える(2026-08-30、変えてはいけない設計)

`lib/Pricing.php`。**比較する2つの値は必ず同じ土俵に揃えること。**

2026-08-30まで、baselineは「全プランの価格をまとめた曜日別の中央値」なのに、
比較相手の`current_price`は「特定の1プランの価格」だった。同じ宿・同じ日でも
プランによって価格は大きく違い(本番で最安31,500/最高77,500という例があった)、
この状態では値下がりではなく**単なるプランのグレード差を拾ってしまう**。
掲載の根拠に関わる歪みで、「割引」表現を避けてきた意味が薄れる。

- baselineは2段階の中央値で作る: (チェックイン日×観測日)ごとの最安値
  → チェックイン日ごとの中央値 → 料金クラスごとの中央値。
  1段目を挟むのは、直前値下げのような一時的な変動に引きずられないため
- `current_price`は「**最新の観測日の最安**」。以前は
  `ORDER BY fetched_at DESC LIMIT 1`だったが、同じ実行で入った複数プランは
  `fetched_at`が同値になるため、どのプランが選ばれるかMySQLの実装依存だった
- `deals.plan_id`もこの最安の行から取る。表示価格とリンク先のプランを
  一致させるため

### 相場は曜日別ではなく平日/休前日の2クラス(2026-08-30、変えてはいけない設計)

`baseline_prices.rate_class`(0=平日, 1=休前日)。金・土のチェックインを休前日、
それ以外を平日として扱う(`rate_class_of()`)。祝日は考慮していない。

曜日別(7分類)だと1分類あたり**週1日しかサンプルが増えず**、本番で判定対象
2,143組のうち**80%がサンプル不足で判定すらできていなかった**。2分類にすると
平日枠は週5日分増えるため、判定できる割合が19.4%→35.1%、deal件数が
11件→146件になった(2026-08-30に実データで確認)。

- `MIN_BASELINE_SAMPLES = 6`は「**根拠になったユニークなチェックイン日の数**」。
  以前は`price_logs`の全行を数えていたため、1日に4プラン取得しただけで
  カウントが4進み、意図した「複数日分の裏付け」になっていなかった
- `baseline_prices`は`price_logs`から毎回作り直せる。集計方法を変えるときは
  中身を捨てて`detect_deals.php`に再計算させればよい

### 乖離率には上限がある(`DEAL_MAX_DISCOUNT_PERCENT = 60`)

下限(25%)だけでなく上限も設けている。運用開始直後はbaselineの根拠になる日数が
少なく、たまたま高値だった時期のデータだけで相場を作ってしまうことがある
(2026-08-30、10日分のデータで61.7%という値が出たのを見て導入)。これを超えるものは
「本当に安い」より「推定が外れている」可能性の方が高く、景表法上も比較対照価格が
不当に高い状態は避けたい。ビジネスホテルの価格変動を考えると50%前後までは
現実にありうるため、明らかに不自然な水準として60%にしてある。

### 表示側でも checkin_date >= CURDATE() で足切りする(2026-08-30)

cronは1日1回(5:30)しか走らないため、**0:00〜5:30の間は前日チェックインのdealが
`is_active=1`のまま残る**。`lib/Deals.php`と`public/deals.php`の一覧系クエリは
すべて`d.checkin_date >= CURDATE()`を付けて表示側でも足切りしている。
新しく一覧を取るクエリを書くときも同じ条件を入れること。

### トップとエリア一覧はホテルごとに1件に絞る(2026-08-30)

乖離率の大きい順に並べると、価格変動の大きい宿が複数日ぶん上位を占めてしまい、
トップの「今週のイチオシ宿」が同じホテルの別日で埋まっていた。選択肢が無いように
見えるため、`get_top_deals()`と`get_deals_by_area()`はホテルごとに乖離率が最大の
1件だけを採用している(別の日付は日付タブから絞り込める)。
`deals.php`の一覧は「条件で絞り込んだ結果の全件」を見せる画面なので重複を許す。

なお本番のMySQLは`ONLY_FULL_GROUP_BY`が有効なので、`GROUP BY d.hotel_id`は使えない。
「ホテルごとに採用する1件のid」をサブクエリで先に決めてからjoinしている。

### 安さの理由文はルールベースのテンプレート選択(2026-08-29、Geminiから移行)

`lib/DealReason.php`(旧`lib/LlmClient.php`)が、宿泊日までの日数・季節性
(夏休み明け・GW明け等)・下落率からカテゴリを判定し、あらかじめ用意した複数
パターンから決定的に1つを選ぶ(ホテル名+宿泊日をシードにするため、同じホテル・
宿泊日なら再計算しても常に同じ文言になる。現在`template-v3`)。

- **Dealは本日〜翌日チェックインに偏るため、文言も`checkin_today`と`last_minute`に
  集中する。**どのカテゴリも最低3パターン用意すること(2026-08-29、本番のdeal 39件が
  全てlast_minuteに寄り2種類の文言しか出ていなかったため拡充した。イチオシ宿を
  6件のカルーセルにしたことで同じ文言が横に並ぶのが目立つようになったのが発端)
- 表示順に依存しない決定的な選択にしているため、隣り合うカードで文言が重なることは
  ありうる。根絶するには表示時の再割り当てが必要になるので、そこは許容している

- Gemini API呼び出しをやめた理由: `TOP_N_PER_AREA`引き上げでホテル数が増え、
  Gemini無料枠のレート制限(1分あたり5リクエスト)に本番で頻繁に引っかかる
  ようになったため
- **「平日は週末より安い」等の当たり前すぎる理由は使わないこと**（ユーザー指摘:
  安さの意外性・お得感が失われる）。季節イベント明け(夏休み・GW・年始・年末)の
  ような、日付から機械的に判定できる時期的な事実に基づく理由に留め、実在しない
  イベント名などを捏造しない
- `GEMINI_API_KEY`の定義自体は`config.php`/`deploy/config.production.php`に
  残っているが、現在どこからも参照されていない(未使用。削除するかは要相談、
  緊急ではない)

### GA4のイベントは用途ごとに分ける(2026-08-30)

以前はエリア選択も日付選択も同じ`find_deals`で送っていて区別できず、
肝心の**楽天への遷移(成果地点)は計測すらしていなかった**。現在は3つ:

| イベント | 発火場所 | 主なパラメータ |
|---|---|---|
| `select_area` | エリア地図 | `area_name` `checkin` `deal_count` |
| `select_checkin` | 日付タブ | `checkin` `days_ahead` `deal_count` |
| `select_hotel` | カードの楽天リンク | `hotel_name` `area_name` `checkin` `price` `discount_rate` |

`select_hotel`がアフィリエイトの入口なので、**カスタムディメンション登録と
コンバージョン設定はこれを最優先で行うこと。**

### CSSはビルドが必要(2026-08-29、忘れると本番だけ壊れる)

Tailwindは`cdn.tailwindcss.com`(実行時にJSでCSSを生成する方式)をやめ、
使っているクラスだけを含む静的CSS(`public/assets/tailwind.css`)を配信している。
CDNは100KB超のJSで初期描画を待たせるため、Core Web Vitalsを考えて移行した。

- **クラスを追加・変更したら必ず`npm run build:css`を実行すること。**
  実行を忘れると、ローカルでは(ブラウザキャッシュ等で)正しく見えても
  本番で新しいクラスのスタイルが当たらない。CSSを触る変更では必ずセットで行う
- ビルド結果`public/assets/tailwind.css`は**Git管理下に置く**。本番はrsyncで
  ファイルを配るだけで、サーバー側にnodeが無いため
- スキャン対象は`tailwind.config.js`の`content`(`./public/**/*.php`と`./lib/**/*.php`)。
  PHPの三項演算子で切り替えるクラスも、文字列としてソースに現れていれば拾われる。
  **クラス名を文字列連結で組み立てると拾われず、本番で消える**
- `node_modules`/`src`/`package-lock.json`は`deploy/conoha-sync.sh`のexclude済み

### デプロイ構成とパス解決の罠

- `deploy/conoha-sync.sh`は、borutomoプロジェクトのSSH鍵(`~/.ssh/conoha-borutomo.pem`)・
  ホスト(`www323.conoha.ne.jp:8022`)・ユーザー(`c2870339`)をそのまま流用している
  (同じConoHa WINGアカウントを使っているため)
- `public/`の中身**だけ**が`~/public_html/bargain.hotelx.tech/`に物理コピーされ、
  アプリ本体(`config.php`/`db.php`/`lib/`)は`~/hotel-bargain/`に別置きになる。
  そのため`public/*.php`から`../db.php`のような相対パスではアプリ本体に届かない。
  `public/bootstrap.php`が、デプロイスクリプトの生成する`public/_app_root.php`
  経由でAPP_ROOTを解決する仕組みになっている。**この仕組みを壊すと本番の全ページが
  Fatal Errorになる**(実際に一度発生させて気づいた)
- シンボリックリンクは使わない。borutomo側でCageFSサンドボックスがシンボリックリンクを
  辿れず403になった実績があるため、同じ物理コピー方式を踏襲している

### デプロイスクリプトはファイル削除も同期する(2026-08-29、変えてはいけない設計)

`deploy/conoha-sync.sh`は`rsync --delete`でローカルの削除を本番にも反映する
(以前は削除が同期されず、`calendar.php`削除時に手動SSHでの後始末が必要になった)。
公開フォルダへの配置も`cp -a`から`rsync --delete`に変更済み。

**新しいexcludeを追加する際は、以下の3つを壊さないこと**（誤って`--delete`で
消してしまった実績がある）:
- `logs`: サーバー側専用のcron実行ログ(discover.log等)。ローカルに存在しない
- `.claude`: ローカル開発用のlaunch.json等。誤って本番に転送された実績あり
- `_app_root.php`: デプロイ時にサーバー側で生成される(public/側のrsyncで
  `--exclude _app_root.php`)

### ホテルのスクリーニングは last_selected_at で絞り込む(変えてはいけない設計)

`discover_hotels.php`はエリアごとレビュー数上位30件(`TOP_N_PER_AREA`、
2026-08-29に12→30へ引き上げ)だけを選び、選んだ行に
`hotels.last_selected_at = NOW()`を書く。`fetch_prices.php`・`detect_deals.php`は
**`last_selected_at`がこの2日以内(`SELECTION_FRESHNESS_DAYS`)のホテルだけ**を対象にする。
選外になったホテルの行自体は削除しない(price_logs等の外部キー制約があり、
過去の価格履歴も残しておきたいため)。

**この列を見ずに`SELECT * FROM hotels`のような全件取得に戻すと、選外ホテルにも
無限にAPIを叩き続ける不具合が再発する。** 2026-08-26に実際にこれで気づかないまま
270件全部にAPIを叩いていた実績がある(discover_hotelsが108件に絞っても、
fetch_pricesは絞り込みを見ていなかった)。

### ConoHa WINGアカウントの共有リスク(変えてはいけない設計)

このアカウント(`c2870339`)は`borutomo.com`含む16ドメインと共有の共有ホスティング。
PHPプロセス枠はアカウント単位で共有されており、2026-08-24に同居WordPress
(`hotelx.tech`)への大量クロール(1日16.8万リクエスト)でborutomo.comが503を起こした
実績がある。**この経緯を踏まえ、hotel-bargainのcrontabは3時間おきではなく
1日1回・深夜帯(4:00〜5:30)にまとめて実行する設計にしている。**
頻度を上げる場合はborutomo.com側への影響を必ず考慮すること。

現在のcrontab(`crontab -l`で確認可能。borutomoの既存行の下に追記してある):
```
0 4 * * *  discover_hotels.php   （エリアごとにレビュー数上位ホテルを発見）
0 5 * * *  fetch_prices.php      （直近1週間分の価格取得。本日〜+7日、2026-08-29に14日から短縮）
30 5 * * * detect_deals.php      （ベースライン算出・Deal判定・理由文生成）
```
`crontab -e`で編集する際は必ず`crontab -l`で既存分(borutomoの`schedule:run`)を
確認してから追記すること。上書きするとborutomoの自動実行が消える。

## 4. 現在直面している課題

### 東京集中方針デプロイの過渡期データがまだ残っている可能性

2026-08-29のデプロイ前に走った旧設定(全国9エリア)のcron実行分が、
`SELECTION_FRESHNESS_DAYS=2日`の猶予により2026-08-31頃まで`fetch_prices.php`/
`detect_deals.php`の対象に残っている(3章「東京集中方針」参照)。フロント表示は
`DISPLAY_PREFECTURE`絞り込みで実害はないが、**次回セッションで本番DBを見て、
東京都以外のhotels/dealsが自然に対象外になっているか確認すること**（本番DBは
ローカルと別物なのでSSH経由で確認、3章「ローカルDBと本番DBは別物」参照）。

### GA4のイベントは受信未確認

タグ設置・本番デプロイ・`window.gtag`が読み込まれていることまでは確認したが、
GA4管理画面のリアルタイムレポートで実際にイベントが届くところまではまだ確認して
いない。イベント名は2026-08-30に`select_area`/`select_checkin`/`select_hotel`へ
作り直してある(3章参照)。なお楽天アフィリエイト管理画面側では2026-08-26以降
クリックの計測自体はできていることを2026-09-01に確認済み(次項参照)。GA4と
楽天側のクリック数を突き合わせられると、`select_hotel`が正しく発火しているかの
裏付けになる。

### 楽天アフィリエイトのクリックはあるが売上(コンバージョン)が0件(2026-09-01時点)

ユーザーが楽天アフィリエイト管理画面のグラフで確認: 2026-08-26以降クリックは
増加傾向(直近ピークで32件/日)だが、売上件数は全期間を通じて0件。

その後、**着地先の不具合が2026-09-04までずっと残っていたことが分かった**
(3章「アフィリエイトURLは『その価格のプラン』に、iPhoneアプリでも日付付きで
飛ばす」参照)。iPhoneで楽天トラベルアプリが起動するとホテルが特定されない、
あるいは日付がブランクのまま開く状態で、スマートフォン経由の予約は事実上
成立しにくかった可能性が高い。**売上0件の主因はこれだった可能性があるので、
2026-09-04以降のコンバージョンの推移を見ること。** 改善しなければ他の要因
(価格の見せ方、掲載しているホテルのターゲット層とのミスマッチ等)を疑う。

なお「アプリが起動すると成果が計測されないのでは」という疑いは、**主因としては
考えにくい**。楽天アフィリエイトのFAQ(`affiliate.faq.rakuten.net/detail/000009819`)は
「同一ユーザーが、クリック後に異なるブラウザで注文した場合、または楽天市場の
スマートフォンアプリで注文した場合は成果報酬の対象」と明記しており、判定は
cookieだけでなくユーザーIDでも行われる。ただしこのFAQが挙げているのは楽天市場
アプリで、**楽天トラベルアプリについて同じと明言されているわけではない**。

### baselineの根拠がまだ10日分しかない

2026-08-30時点で、baselineの根拠になっているチェックイン日は最大11日分。
過去60日を見る設計だが運用日数が足りていない。日が経つほど相場の推定は
安定するはずなので、**deal件数や乖離率の分布が落ち着くまでは、
`DEAL_THRESHOLD_PERCENT`や`DEAL_MAX_DISCOUNT_PERCENT`をいじらずに様子を見ること。**
現在は東京都で145件、乖離率は25〜55%に分布している。

### `GEMINI_API_KEY`が未使用のまま残っている

理由文生成をルールベースのテンプレート選択に移行した(3章参照)ため、
`GEMINI_API_KEY`の定義(config.php/deploy/config.production.php)を参照する
コードが無くなった。削除するかは要相談(緊急ではない)。

## 5. 次に着手すべきタスク(Next Actions)

優先度順。

1. **楽天アフィリエイト管理画面で売上件数(コンバージョン)の推移を追う。**
   2026-09-04にiPhoneでの着地先不具合を解消したので、ここから数日〜数週間で
   変化が出るかが最大の関心事(4章参照)。クリック数は8/26以降ずっと出ている
2. 次回cron実行後、本番DBの`hotels.booking_url`が新方式
   (`hb.afl.rakuten.co.jp/hgc/.../?pc=…/HOTEL/{番号}/{番号}.html&m=…`)に
   置き換わっているか確認する。表示側で正規化しているため旧形式が残っていても
   リンク自体は正しく動くが、方式が本番に行き渡っているかの確認として
3. GA4のリアルタイムレポートでイベント受信を確認する。**`select_hotel`が
   アフィリエイトの入口なので最優先**。届いていたら`hotel_name`/`area_name`/
   `price`/`discount_rate`をカスタムディメンションとして登録し、
   `select_hotel`をコンバージョンとしてマークする(3章「GA4のイベントは
   用途別に分ける」参照)
4. deal件数と乖離率の分布を数日追う。2026-08-30に判定ロジックを大きく変えた
   直後(11件→145件)で、baselineの根拠もまだ10日分しかない(4章参照)。
   **落ち着くまで閾値はいじらないこと**
5. Search Consoleの「ページ」レポートで、エリアページ9本がインデックス登録済みに
   なるかを数日後に確認する。登録・サイトマップ送信は2026-08-29に完了済みで、
   サイトマップの全12URLがHTTP 200・noindexなし・canonical自己参照であることは
   確認してある
6. 東京集中方針の過渡期データが解消されたか確認する(4章参照)
7. (方針変更があれば)2名宿泊版を作る場合の判断材料: **API使用量は倍**になり
   (3章参照)、`price_logs`に人数の列を足したうえでbaselineを貯め直す必要がある。
   `MIN_BASELINE_SAMPLES`は「ユニークなチェックイン日数」なので、平日枠なら
   1〜2週間程度で揃う見込み(2026-08-29に「約6週間」と記録したのは、
   sample_countの意味を誤解していたための誤り)

## 6. 作業の進め方(このプロジェクトでの約束)

- 本番反映(`deploy/conoha-sync.sh`の実行)は必ず事前に確認を取る
- GitHubへのpush・PR作成・マージも必ず事前に確認を取る
- 「割引」「OFF」「セール」は使わない。「相場より○%安い」で統一する
- `config.php` / `deploy/config.production.php`はGit管理外。値を一時的に書き換えて
  凌いだ場合は、作業完了後に必ず元の値へ戻す
- **hotel-bargain-phpで作業を始める前に必ず`git pull`する。** このリポジトリは
  別セッションからもコミットされており、ローカルの`main`は平気で数日ぶん遅れている。
  2026-09-04に古い`main`からブランチを切ってPRを出し、衝突してクローズし直す羽目に
  なった(#40)。「手元のCLAUDE.mdに書いていない＝まだ手が付いていない」ではないので、
  同じ問題に着手する前に`git log --oneline`で直近のコミットを見ること
